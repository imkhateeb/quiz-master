const Question = ({ question }) => {
  return (
    <div className="text-darky font-semibold font-poppins">
      <div>{question}</div>
      <div>Speak --- </div>
    </div>
  );
};

export default Question;
